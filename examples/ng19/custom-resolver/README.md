# Custom Resolver

This example shows how to work with a custom component resolver. You can dynamically switch the component that is used for the "choice" control. It can be toggled between Radio Buttons and Dropdowns
